# Dependances d'execution. Windows uniquement pour l'essentiel : la barre
# des taches, la fenetre active et le Bluetooth passent par des API Windows.
bleak
psutil
pystray
pillow
mss
numpy
soundcard
pywin32; sys_platform == "win32"
# La dictee locale (le moteur de Handy : Parakeet V3 en ONNX). Le modele,
# lui, n'est pas dans l'exe : repris de Handy ou telecharge a la demande.
onnxruntime
onnx-asr
